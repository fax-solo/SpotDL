---
description: Build and deploy the frontend to Cloudflare Pages
agent: build
---

Run these steps:
1. `cd frontend`
2. Run the build: `npm run build`
3. Run the deploy: `npm run cf:deploy`
4. Report the deployment URL back to the user
