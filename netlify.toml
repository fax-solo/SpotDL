[build]
  base = "frontend/"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = ""

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
